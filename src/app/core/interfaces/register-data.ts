export interface RegisterData {
    name: string;
    email: string;
    password: string;
    rePassword: string;
    phone: number;
}

export interface LoginrData {
    email: string;
    password: string;
}
